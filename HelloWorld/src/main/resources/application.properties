# ==============================================================
# PROJECT CORE SETTINGS
# ==============================================================
spring.application.name=HelloWorld

# ==============================================================
# DATABASE: SUPABASE CLOUD (SEOUL POOLER)
# ==============================================================
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&connectTimeout=10
spring.datasource.username=postgres.jqvbhzvbgyubgberqsan
spring.datasource.password=keepGoing123452026
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# JPA / HIBERNATE OPTIMIZATION (THE SPEED FIX)
# ==============================================================
# Set to 'none' because your tables are already created.
# This saves ~2 seconds of startup scanning!
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# ==============================================================
# CONNECTION POOL (HIKARI)
# ==============================================================
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.connection-timeout=5000

# ==============================================================
# PRODUCTION FLAGS
# ==============================================================
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false